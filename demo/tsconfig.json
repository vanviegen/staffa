{
  // Type-checks and compiles the demo against Skye's *built* types, mirroring the
  // import map in index.html (which resolves "skye" -> ../dist/index.js at
  // runtime). Run `npm run build` first so ../dist exists.
  //
  // Self-contained (rather than extending ../tsconfig.json) because the demo
  // compiles in place — demo.ts -> demo.js next to it — so it must not inherit the
  // library's rootDir/outDir.
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "paths": {
      "skye": ["../dist/index.d.ts"]
    }
  },
  "include": ["demo.ts"]
}
